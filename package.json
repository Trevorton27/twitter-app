{
  "name": "twitter-app",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm run watch --prefix client",
    "dev": "concurrently \"nodemon app.js\" \"npm run client\"",
 "heroku-postbuild": "cd client && npm install && npm run build",
    "start": "node app.js",
    "build": "node app.js && cd client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.12"
  }
}
